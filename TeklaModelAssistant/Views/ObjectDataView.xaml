<UserControl x:Class="TeklaModelAssistant.Views.ObjectDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeklaModelAssistant.Views"
             xmlns:cm="http://caliburnmicro.com"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:ObjectIdentificationView cm:Bind.Model="{Binding ObjectIdentificationViewModel}" Grid.Row="0"/>
        <local:ObjectAttributesView cm:Bind.Model="{Binding ObjectAttributesViewModel}" Grid.Row="1"/>
        <local:ObjectGeometryView cm:Bind.Model="{Binding ObjectGeometryViewModel}" Grid.Row="2"/>
    </Grid>
</UserControl>
