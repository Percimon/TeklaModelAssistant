<UserControl x:Class="TeklaModelAssistant.Views.PolybeamGeometryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeklaModelAssistant.Views"
             mc:Ignorable="d" 
             >
    <StackPanel>

        <DataGrid x:Name="Points" AutoGenerateColumns="False" CanUserAddRows="False" AlternatingRowBackground="Gray" AlternationCount="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="X" Binding="{Binding X}"/>
                <DataGridTextColumn Header="Y" Binding="{Binding Y}"/>
                <DataGridTextColumn Header="Z" Binding="{Binding Z}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button x:Name="UpdateCoordinates" Content="Update coodrdinates"/>
    </StackPanel>
</UserControl>
